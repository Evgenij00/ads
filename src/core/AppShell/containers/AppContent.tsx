import React from 'react'
import { Router } from '../components'

export const AppContent = (): JSX.Element => {
  return (
    <div>
        <div>AppContent</div>
        <Router/>
    </div>

  )
}
